#include <bits/stdc++.h>

#define endl '\n'
#define pb push_back
#define mp make_pair


int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n;
	cin >> n;
	vector<int> pts(n);
	for(int i = 0; i < n; ++i)
	{
		int x;
		cin >> x;
		pts[i] = x;
	}
	
}