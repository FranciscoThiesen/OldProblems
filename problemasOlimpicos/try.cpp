#include <cstdio>
#include <string>
#include <algorithm>
#include <cmath>

int main()
{
    int a;
    scanf("%d", &a);
    for(int i = 0; i < a; ++i)
        printf("%d\n", i);
    return 0;
    // ronaldo
}
